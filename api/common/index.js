import { http } from uni.$u
export function useCommonApi() {
	return {
		add: (params = {}) => {
			// return request();
		},
		update: (params = {}) => {
			// return request();
		},
		del: (params = {}) => {
			// return request();
		},
		detail: (params = {}) => {
			// return request();
		}
	};
}